import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primary = Color(0xFF0052CC);
  static const Color primaryDark = Color.fromARGB(255, 0, 114, 195);
  static const Color primaryLight = Color.fromARGB(255, 7, 74, 175);

  // Colores de fondo
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);

  // Colores de texto
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textTertiary = Color(0xFF9CA3AF);

  // Colores de input
  static const Color inputBorder = Color(0xFFE5E7EB);
  static const Color inputFocusedBorder = Color(0xFF6366F1);
  static const Color inputFill = Color(0xFFFAFAFA);

  // Colores de estado
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Colores de sombra
  static const Color shadow = Color(0x1A000000);
  static const Color shadowLight = Color(0x0D000000);

  // Google Colors
  static const Color google = Color(0xFF4285F4);
  static const Color googleBorder = Color(0xFFDADBDD);
}
